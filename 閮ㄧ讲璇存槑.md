# 把健康管理小助手装到手机上

**结论：不需要做微信小程序。** 把这几个文件传到一个免费的静态托管上，拿到一个网址，在手机上「添加到主屏幕」，就会得到一个有图标、全屏打开、**断网也能用**的应用。全程约 10 分钟，之后每次改动只要替换一个文件。

---

## 这个包里有什么

| 文件 | 作用 | 会不会经常改 |
|---|---|---|
| `index.html` | 应用本体，全部内容都在这一个文件里 | ✅ 每次更新只改它 |
| `manifest.json` | 告诉手机应用叫什么名字、用什么图标、全屏打开 | 基本不用动 |
| `sw.js` | 离线缓存。第一次打开后即可断网使用 | 改版时把版本号 +1 |
| `icon-192.png` / `icon-512.png` / `apple-touch-icon.png` | 主屏幕图标 | 不用动 |

**六个文件必须放在同一层目录**，不要再套一层文件夹。

---

## 第一步：托管（三选一）

### 方案 A｜GitHub Pages（推荐，长期稳定、国内可访问）

1. 注册 github.com 账号
2. 右上角 `+` → `New repository`，名字随便取（例如 `health`），选 **Public**，创建
3. 进入仓库 → `Add file` → `Upload files` → **把六个文件一起拖进去** → `Commit changes`
4. `Settings` → 左侧 `Pages` → Source 选 `Deploy from a branch`，分支选 `main`、目录选 `/ (root)` → Save
5. 等 1–2 分钟，页面顶部会出现网址，形如
   `https://你的用户名.github.io/health/`

### 方案 B｜Netlify Drop（最快，但要注册）

打开 `app.netlify.com/drop` → 登录 → **把整个文件夹拖进网页** → 立刻得到一个网址。适合想先试一下的情况。

### 方案 C｜腾讯云 / 阿里云对象存储静态托管

国内访问最快，但需要实名和少量配置费用。除非你觉得前两个太慢，否则不必。

> **关于访问速度**：因为有离线缓存，只有**第一次打开**和**更新时**需要联网。之后即使在地铁里、医院地下室，照样能打开。所以托管在哪里其实不太影响日常体验。

---

## 第二步：添加到主屏幕

### iPhone（必须用 Safari，微信里打开不行）

1. Safari 打开你的网址
2. 底部**分享按钮**（方框向上箭头）
3. 下滑找到 **「添加到主屏幕」**
4. 名称会自动填「健康助手」→ 右上角「添加」

主屏幕上会出现图标，点开是全屏的，没有浏览器地址栏。

### 安卓

Chrome 打开网址 → 右上角三个点 → **「安装应用」** 或「添加到主屏幕」。

---

## 第三步：把提醒装进日历

打开应用 → **「时间轴」页签** → 点 **「导出日历文件（.ics）」**

- iPhone：会跳出用「日历」打开，选「添加全部」
- 安卓：文件下载后，在 Google 日历「设置 → 导入」中选择

**这一步不能省。** 网页本身不会主动提醒你——iOS 上的网页推送需要架推送服务器，为一个人用的工具不值得。所以分工是固定的：

| 层 | 谁负责 |
|---|---|
| **到点弹出提醒** | 手机系统日历（.ics） |
| **每天查看、勾选、记录** | 这个应用 |
| **长期档案沉淀** | 你的中文健康管理文档 |

---

## 以后怎么更新

内容有变化时（比如复诊后计划调整）：

1. 我给你新的 `index.html`
2. 把 `sw.js` 里第 3 行的 `hm-v1` 改成 `hm-v2`（下次再改就 `hm-v3`，以此类推）
3. 把这两个文件重新上传，覆盖旧的
4. 手机上打开应用 → 完全关掉 → 再打开，就是新版

**第 2 步不能跳过。** 不改版本号的话，手机会一直用缓存里的旧版本，你会以为更新失败。

---

## 常见问题

**Q：数据存在哪里？会不会丢？**
存在手机浏览器本地，不上传任何服务器。但要注意：**清除 Safari 数据、卸载重装应用，勾选记录会一起没**。所以重要的症状记录，用应用里的「复制最近 14 天记录」导出，粘贴到备忘录留一份。

**Q：能不能在电脑上也看？**
可以，同一个网址电脑浏览器直接打开。但勾选记录是按设备存的，两边不同步。

**Q：微信里打开为什么不能添加到主屏幕？**
微信内置浏览器不支持这个功能。发给自己后，用右上角「···」→「在 Safari 中打开」。

**Q：一定要 https 吗？**
是的。离线缓存（Service Worker）只在 https 下工作。上面三个托管方案默认都是 https，不用额外配置。

---

*应用内容为个人执行参考，不构成医疗建议。所有时间点与用药以各科医嘱为准。*
